# عقد عمل Doraemon وCodex

هذا الملف يحدد طريقة تنظيم الملاحظات دون تغيير أصل الكتابة بالقلم.

## عند طلب «رتّب آخر ملاحظة»

1. اقرأ ملفات `data/notes/*.json` من مستودع البيانات الخاص.
2. اختر أحدث ملاحظة تحقق `organization.state === "queued"` حسب `organization.requestedAt`، ما لم يحدد المستخدم ملاحظة أخرى.
3. افتح معاينة الكتابة المقابلة في `data/previews/<note-id>.webp` لقراءة الخط والرسم بصرياً.
4. لا تغيّر `strokes` أو `drawing` أو `createdAt`.
5. استخرج من المحتوى المتاح:
   - ملخصاً قصيراً في `summary`.
   - مهام واضحة في `tasks` مع `id` فريد و`done: false`.
   - المواعيد في `task.due` عندما تكون صريحة.
   - المشروع المناسب في `projectId` فقط عند وجود مشروع مطابق أو بعد إنشاء مشروع حقيقي في `data/projects.json`.
6. حدّث الملاحظة إلى:
   - `status: "organized"`، أو `"linked"` إذا رُبطت بمشروع.
   - `organization.state: "organized"`.
   - `organization.organizedAt` إلى توقيت ISO الحالي.
   - `updatedAt` إلى توقيت ISO الحالي.
7. اقرأ SHA الحالي للملف قبل الكتابة ولا تستبدل نسخة تغيرت أثناء العمل. عند التعارض احتفظ بالنسختين أو اطلب الدمج.

## قواعد الأمان

- مستودع الواجهة العام لا يستقبل ملاحظات المستخدم.
- لا تُكتب رموز الوصول في الملفات أو السجل أو الردود.
- لا تحذف الكتابة الخام بعد التنظيم.
- لا تنشئ مهام أو مشاريع تخمينية لا يدل عليها المحتوى.
